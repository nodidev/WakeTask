// Root-level build file. This just declares WHICH versions of the Android and
// Kotlin plugins are available to the modules below (right now, just ":app").
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
